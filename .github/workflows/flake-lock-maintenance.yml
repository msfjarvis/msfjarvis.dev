name: Update flake.lock
on:
  workflow_dispatch:
  schedule:
    - cron: '0 6 * * 0'
permissions:
  contents: write
jobs:
  update-flake-lock:
    uses: msfjarvis/shared-workflows/.github/workflows/update-flake-lock.yml@b205c03a32d56f5b256d41dd18d44398e07e7332 # main
    secrets:
      github-token: ${{ secrets.GITHUB_TOKEN }}
