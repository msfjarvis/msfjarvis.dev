---
import ImageModal from './ImageModal.astro';

interface Props {
  src: ImageMetadata | string;
  alt?: string;
  title?: string;
  small?: boolean;
}

type ImageMetadata = {
  src: string;
  width: number;
  height: number;
  format: string;
};

const { src, alt = '', title, small = false } = Astro.props;
---

{typeof src === 'object' ? (
  <ImageModal src={src} alt={alt} title={title} small={small} />
) : (
  <figure>
    <img class={small && "half" || ""} src={src} alt={alt} />
    {title && <figcaption>{title}</figcaption>}
  </figure>
)}
